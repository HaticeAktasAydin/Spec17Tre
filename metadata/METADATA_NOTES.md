# Spec17Tre Metadata Notes

- `class_mapping.csv` is ready for publication.
- `dataset_statistics.csv` contains the sample and feature counts reported in the published article.
- `feature_description.csv` contains 399 rows, but only representative feature families are currently documented.
  The exact gem5 statistic names must be imported from the official raw dataset header before public release.
- Placeholder rows are deliberately marked `pending` to avoid inventing or misrepresenting feature names.
